specVersion: "v1beta"
serviceId: "chatapplication"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "chatapplication-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
